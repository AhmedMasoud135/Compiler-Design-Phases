# Compiler-Design-Phases
Python-based compiler design project providing lexical analysis, multiple parsing techniques (LL(1), LR(0), SLR(1)) with grammar editing, DFA and trace visualization, plus semantic analysis with AST, symbol table, and type/scope checking for a C-like language.
